import styled from 'styled-components';

const PasswordRecoveryPageContainer = styled.div`
  background-color: ${(props) => props.theme.background.main};
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
`;

export default PasswordRecoveryPageContainer;
