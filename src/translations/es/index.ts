import { Dictionary } from '../dictionary';

// eslint-disable-next-line camelcase
const global_es: Dictionary = {
  "header": {
    "sign_off" : "Cerrar sesión",
  },
};

// eslint-disable-next-line camelcase
export default global_es;
