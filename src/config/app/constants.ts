// eslint-disable-next-line import/prefer-default-export
export const baseUrl = process.env.TEST_API;

export const STATUS = {
  error: 'error',
  loading: 'loading',
  success: 'success',
  idle: 'idle',
};

export const GENERIC_ERROR_MESSAGE = 'Ha ocurrido un error inesperado';

